puppet-installserver (WIP)
==========================

Standalone manifest to deploy an install-server


How to test with Vagrant ?
==========================

Just run :
```
vagrant up
```

or you can just run install.sh on a freshly installed node :

```
/bin/sh install.sh
```
